### 获取url 如:路由的 route('admin.role') = admin/role

    Route::current()->uri();
    
### Call to a member function setCookie() on null

    Laravel 的中间件里是不能直接返回视图的，要使用 response 的 view 方法返回
    return response()->view('error_page');
    
### 子iframe页面刷新父页面

    parent.location.reload();
    
### 获取所有路由相关信息

    $app = app();
    $routes = $app->routes->getRoutes();
    foreach ($routes as $k=>$value){
         $path[$k]['uri'] = $value->uri;
    
         $path[$k]['path'] = $value->methods[0];
    
    }
    dd($path);